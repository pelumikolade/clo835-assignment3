#Deploy Loadbalancer Service
apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    name: app-lb-service
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: employees
  type: LoadBalancer